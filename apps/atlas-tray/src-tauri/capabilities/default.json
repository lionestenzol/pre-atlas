{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability set. The tray + window logic runs entirely in Rust (app.exit, window.hide, window.show), so no JS-side IPC permissions are needed beyond core defaults.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
