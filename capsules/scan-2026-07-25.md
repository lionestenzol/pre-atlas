# SUPERSEDED — 2026-07-25

**This file's original content ("candidates for closure") reflected the wrong doctrine.** Worktrees are not mudball to be closed — they are project bookmarks: what did I do, what's left, did I finish. Read [BOOKMARKS.md](BOOKMARKS.md) instead.

## Doctrine (locked 2026-07-25)

- **Central index only.** One file — `capsules/BOOKMARKS.md`. Zero writes into worktree directories.
- **Done? = merged to main OR tagged as `capsule/*`.** Everything else is OPEN — a bookmark to revisit.
- **Nothing gets deleted.** Not the worktree, not the branch, not the tag. Capsule tags freeze the tree permanently even if the working directory is later removed.
- **Read back-to-front.** Most-recently-touched OPEN bookmarks first; oldest at the bottom (likely dead or forgotten but still surface-able).

## Regenerate

```bash
python scripts/scan_worktree_bookmarks.py
```

## Followups still pending

- `services/delta-kernel/src/governance/closure_daemon.ts` still generates scan files with the old "candidates for closure" framing. Next daemon touch should either invoke `scan_worktree_bookmarks.py` or be rewritten to output BOOKMARKS.md rows directly. Cron fires Monday 07:00 — has time to fix before the next run.
- Two worktree directories (`adoring-rubin-7f09bf`, `interesting-chaum-2944a8`) were deleted this session before the doctrine was named. Their branches + capsule tags are preserved; the browsable directories can be restored the moment disk space frees up via `git worktree add`.
