{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "CognitiveMetricsComputed.v1",
  "title": "Cognitive Metrics (Computed)",
  "description": "Metrics computed from conversation analysis. Source: cognitive-sensor/cognitive_api.py",
  "type": "object",
  "required": ["state", "loops", "drift", "closure"],
  "properties": {
    "state": {
      "type": "object",
      "required": ["first_activity", "last_activity", "total_convos"],
      "properties": {
        "first_activity": {
          "type": "string",
          "format": "date",
          "description": "Date of first recorded conversation"
        },
        "last_activity": {
          "type": "string",
          "format": "date",
          "description": "Date of most recent conversation"
        },
        "total_convos": {
          "type": "integer",
          "minimum": 0,
          "description": "Total number of conversations in database"
        }
      }
    },
    "loops": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["convo_id", "title", "score"],
        "properties": {
          "convo_id": {
            "type": "string",
            "description": "Conversation identifier"
          },
          "title": {
            "type": "string",
            "description": "Conversation title"
          },
          "score": {
            "type": "number",
            "description": "Abandonment score (higher = more likely unfinished)"
          }
        }
      },
      "description": "Top open loops by abandonment score"
    },
    "drift": {
      "type": "object",
      "additionalProperties": {
        "type": "number"
      },
      "description": "Topic weights from recent conversations (last 90 days)"
    },
    "closure": {
      "type": "object",
      "required": ["open", "closed", "ratio", "closure_quality"],
      "properties": {
        "open": {
          "type": "integer",
          "minimum": 0,
          "description": "Number of open loops"
        },
        "closed": {
          "type": "integer",
          "minimum": 0,
          "description": "Total decisions made (CLOSE + ARCHIVE)"
        },
        "truly_closed": {
          "type": "integer",
          "minimum": 0,
          "description": "Loops with CLOSE decision (actually finished)"
        },
        "archived": {
          "type": "integer",
          "minimum": 0,
          "description": "Loops with ARCHIVE decision (parked, not done)"
        },
        "ratio": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Decision ratio: % of backlog triaged (decisions / total * 100)"
        },
        "closure_quality": {
          "type": "number",
          "minimum": 0,
          "maximum": 100,
          "description": "Quality ratio: % of finished loops truly CLOSED vs just ARCHIVED. Below 30% triggers CLOSURE mode."
        }
      }
    }
  }
}
