# BEARINGS — 2026-06-25 (end of day)

> **What this is.** The accurate "where everything stands" snapshot, written by
> reconciling git (ground truth) against memory + the committed docs and
> verifying claims against code. It **supersedes `NEXT_SESSION_HANDOFF.md`** for
> current state — that file is a month-old (2026-05-28) corpus-mining handoff that
> was swept into today's docs commit and does **not** describe today's work.
>
> Companion: `REPO_RUNDOWN.md` (structure/ports/LOC, reconciled 06-22) is still the
> trusted repo map. This doc is the *temporal* layer on top of it.

---

## 1. What today actually was

A ship-and-commit day: ~30 commits on Pre Atlas (07:40→11:18) plus commits in three
sibling repos. The morning ended with a **"commit everything" sweep (~11:05–11:18)**
that landed all the work memory still tags "(uncommitted)".

**Pre Atlas** — branch `feat/atlas-setup-ui`, by theme:
- **droplist** — lifecycle bricks 1–4 + ship/harden + a11y + always-on daemon + TOCTOU fix + autopilot flag (`8e702e8`, `fe44b06`, `e8bd3f6`, `a9f1a45`, `fe03142`…)
- **lattice** — Week 2 layouts + Week 3 (TinyBase rejected, single source of truth) + dead-CSS removal + Playwright smoke (`7289051`, `0df6d01`, `b9293f0`)
- **atlas-map-api** — clickable Setup UI + launchables/halt, write-token scoping (HIGH sec), :3074→:3075 collision fix, `/call` X-Atlas-Token forwarding (`4aefa10`, `8f0a32c`, `a20e709`)
- **delta-scp-web** UI (`4592ccb`), **hydra** digestion engine (`2ae0365`), **inpact** in-app AI assistant (`61ba796`), **Atlas Setup profile** (`029ec51`)
- **chore/audit/docs** sweep: system-index refresh + vendored cytoscape + 15 doc files (`dda821d`, `d68fd65`)

**Sibling repos** (each on its own branch, trees clean except STRUDEL):
- `C:\Users\bruke\pre-atlas` (hyphen) — delta-scp Supabase-free demo gateway (`87e4679`, branch `feat/delta-scp-v2-graph-memory`)
- `C:\Users\bruke\anatomy-saas` — deterministic compiler core, seq 01 (`e87df2e`, `master`)
- `C:\Users\bruke\STRUDEL` — website static-build repair committed (`8e1e19e7`, branch `enterprise-hardening`); **uncommitted**: 11 new `my-patterns/*.md`, `website/src/pages/assembler.astro`, README/package.json + `.audit/` artifacts. **Not in Pre Atlas memory at all.**

---

## 2. ⚠️ The landing problem (the bearing that matters most)

`feat/atlas-setup-ui` is **126 commits ahead of `main`** (and 16 behind). Essentially
the entire week+ of work lives on this one feature branch. **Built ≠ Official.**

**Landing plan (proposed):**
1. **Land droplist via PR #24** — already focused, off `main`, conflict-free, 67 tests. The one clean merge on record. Click it.
2. **Do NOT resurrect PR #23** (355 files, conflicting, parked). It's not the path.
3. **Carve the rest into focused PRs off `main`** by theme, in dependency order:
   `atlas-map-api` (setup UI + gateway) → `lattice` (W2/W3) → `delta-scp-web` →
   `hydra` / `inpact` / `setup-profile`. Each its own branch + green tests, like #24.
4. **Reconcile the 16 commits `main` has that the branch doesn't** before/while branching
   (rebase or cherry-pick forward) so PRs apply cleanly.
5. **Decide the retired-systems prune** (`mirofish`, `openclaw`, `mosaic-*`,
   `blueprint-generator`, `ai-exec-pipeline`) as a separate deletion PR — see REPO_RUNDOWN §7/§9.

---

## 3. Doc trust map

| Trust | File | Note |
|---|---|---|
| ✅ Bearings (this) | `BEARINGS_2026-06-25.md` | temporal snapshot |
| ✅ Structure | `REPO_RUNDOWN.md` | 35 systems, ports, LOC, seams (06-22) |
| ✅ Current | `SHIP_READINESS_AND_MARKET_2026-06-25.md`, `SAAS_SHIP_PLAN_2026-06-25.md`, `DROPLIST_SHIP_SPEC` | today's ship thinking |
| ⚠️ Stale trap | `NEXT_SESSION_HANDOFF.md` | 05-28 corpus-mining; NOT today |
| ⚠️ Stale | `HANDOFF_AUTOSTART.md` | chore closed 06-15 (`92c686a`) — delete |
| 🕸️ Superseded | ~18 Jan/Apr docs (`CODEBASE_ANALYSIS`, `FILE_MAP`, `ARCHITECTURE_BRIEFING`, `PRE_ATLAS_MAP`, `BACKEND/FRONTEND_AUDIT`…) | pre-date REPO_RUNDOWN |

---

## 4. Memory drift corrected (now true)
- Memory "(uncommitted)" tags on **Atlas Setup profile** (`029ec51`), **Hydra eating engine**
  (`2ae0365`), **self-describing `/call` gateway** (`a20e709`/`072d868`), **system-index refresh**
  (`dda821d`) → all **committed today** on `feat/atlas-setup-ui`. They read as open; they're done-on-branch.
- **STRUDEL** has real work with zero memory coverage (see §1).

---

## 5. Asymmetric-leverage project (tool-outcome ledger + router) — VERIFIED state

**Lives at `~/.claude/scripts/ledger/`.** Verified against code 2026-06-25 (not memory):

| Brick | DOD | Real status |
|---|---|---|
| 1 Backfill | mine transcripts → ledger | ✅ DONE, gated. `tool-outcomes.jsonl` = **74 rows** |
| 2 Enrichment | shipped/retried/fused `reward_score` | ❌ **not built** (the real remaining build) |
| 3 Live hook | `append_outcome.py` grows ledger at session end | ✅ **WIRED + verified idempotent 2026-06-25** (Stop hook, fail-safe) |
| 4 Router | `router.py` `pick_tool()` Thompson bandit | 🟡 **code exists, FAILS proof** |

**Honest verification run (`python router.py --evaluate`):**
```
router_expected_reward : +0.3000
random_expected_reward : +0.3875
margin                 : -0.0875   →  router does NOT beat random
```
And `router.py suggest` currently recommends `/schedule` off a single **n=1** exploration
row — noise, not earned reward. Cause: ledger is thin (74 rows, ~half neutral) and reward is
sentiment-only. **The router is built but not yet valuable.**

**Path to value (the actual spec — do in a fresh session):**
1. ~~Wire the Stop hook~~ ✅ **DONE 2026-06-25** — `append_outcome.py` appended to the
   `settings.json` `Stop` array (async, fail-safe), settings re-validated, idempotency verified
   (`added 0` twice on an already-mined transcript). Brick 3 closed; ledger now grows live.
2. **Build Brick 2 enrichment** — add `shipped` (git commit appears later in session),
   `retried` (same skill re-fired = first attempt failed), fuse into `reward_score ∈ [-1,1]`.
   `router.py` already *reads* `reward_score` first, so this is plug-in.
3. **Re-run `router.py --evaluate`** — only "done" when margin > 0 on holdout (DOD B4.3).
4. **Surface it (B4.4)** — wire `router.py suggest` into a decision point (boot header / a skill),
   but **gate on n** so it never recommends an n=1 arm.

**es conv-mining + session template (the analytical half of the ask):**
- Backfill already mines `~/.claude/projects/**/*.jsonl` for tools-run + reward — reuse it; don't
  re-roll a miner. For "today's work / input-vs-output / patterns", filter the ledger + transcripts
  to `dm:today` sessions (`es dm:today path:"C:\Users\bruke\.claude\projects" ext:jsonl -p`).
- Output = a **session-template proposal**: which openings/tool-orders correlated with POS reward,
  which with retries/NEG. That template + the gated router suggestion = the "asymmetric leverage" loop.

---

## 6. Cleanup backlog (low-risk, when in CLOSURE)
- **26 abandoned `claude/*` worktrees** in `.claude/worktrees/` — prune (`git worktree remove`).
- **Root junk:** `rootfestival-project…_PLAN…md` ×4, untracked `forensic-map.md` — delete.
- **Stale docs:** `NEXT_SESSION_HANDOFF.md`, `HANDOFF_AUTOSTART.md` (see §3).

---

## 7. Next-session fire-order (suggested)
1. Click **droplist PR #24** (the one clean landing).
2. ~~Wire the ledger Stop hook~~ ✅ done — data now compounds automatically.
3. **Carve PR #2** off main (atlas-map-api) per §2 landing plan.
4. Fresh build session: **Brick 2 enrichment** → re-eval router.
5. STRUDEL: commit the 11 pattern docs + `assembler.astro` (own repo, own session).
