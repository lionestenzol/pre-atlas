{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd services/delta-kernel/web && npm install && npm run build",
  "outputDirectory": "services/delta-kernel/web/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
